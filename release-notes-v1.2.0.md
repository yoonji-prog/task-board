# v1.2.0 릴리즈 노트

업무 트래커가 단순 상태 보드에서 **프로세스 관리 + 링크 허브 + 보고 도구**로 확장됐습니다.

## 주요 변경

**프로세스 6단계 관리**
기획 → 디자인 → 마크업 → 마크업 필터링 → 개발 → 개발 필터링. 업무마다 필요한 단계만 켜고 끌 수 있고, 진행률이 자동으로 계산됩니다.

**기획 단계 전용 상태값**
다른 단계(예정/진행/완료)와 달리, 기획 단계는 **예정 → 디자인 리뷰 → 개발 리뷰 → 완료** 로 진행됩니다. 배지를 누르면 순환합니다.

**업무별 링크 허브**
카드에 지라·기획서·테스트 페이지 등 링크를 추가/열기/수정/삭제할 수 있습니다. 종류(기획서·디자인·마크업·개발·기타)별로 묶어서 보여줍니다.

**주간보고에 링크 포함 + 리치텍스트 복사**
붙여넣으면 라벨은 클릭 가능한 하이퍼링크로, URL은 그대로 살아있습니다. 한 항목에 링크가 여러 개면 쉼표(,)로 구분됩니다.

**날짜 형식 통일**
주간보고와 카드 기간 표기를 `MM/DD` 슬래시 형식으로 통일했습니다. 주간보고는 `(MM/DD(요일))` 형태로 나옵니다.

**인라인 편집**
카드에서 기간·특이사항·메모를 클릭해 바로 수정할 수 있습니다.

**완료 업무 상단 전환 보기**
상단 '완료 업무' 카드를 클릭하면 그 자리에서 완료 목록으로 전환됩니다. 카테고리 필터도 함께 적용됩니다.

**Notion 체크리스트 복사**
현재 목록을 Notion 투두 마크다운으로 복사합니다.

## 수정
- 드래그를 전용 손잡이(⠿)로 분리해 카드 안 편집이 정상 동작하도록 수정
- 메모 열림 조건 타입 비교 버그 수정
